# Mode 19 摩擦辨识迁移与实机验证（2026-09-05）

## 结论

主线 `74cbcaa` 的 Mode 19 摩擦辨识已按当前分层架构迁移，主线 `5e88066` 的最高辨识速度调整也已同步为 `0.5 rev/s`。在 28 V 母线、约 1.5 N·m 阻尼环、固件电流限幅 6 A 条件下连续完成两轮 8/8 个正反速度点。第一轮只验证候选值，第二轮执行应用和保存；J-Link 复位后参数保持，控制器为 Mode 0、Error 0。

## 参数兼容验证

schema 从 v8 升级到 v9 后，加载器会校验旧双槽记录的产品、硬件、电机、schema、长度和 CRC，再把 v8 有效负载迁移到 v9 运行时对象。实机按“v8 基线 → 下载 v9 固件 → 保存为 v9 → 复位”验证：

- 编码器在线状态、方向、极对数、电流限幅、速度限幅、Rs、Ld、Lq 和磁链均保持；
- 三次导出的 1024 点 LUT SHA-256 均为 `23BA063CBC2127B375A8C85F5DD2033E039C5C98F7AA6690A4C48AB4B5BA9F21`；
- v9 保存并复位后 LUT 逐点差异为 0；
- 旧 schema 不包含摩擦模型，因此 `friction_model_valid=0`，不会误启用补偿。

## Mode 19 结果

| 目标速度 (rev/s) | 平均速度 (rev/s) | 绝对跟踪误差 | 平均 Iq (A) | 样本数 |
| ---: | ---: | ---: | ---: | ---: |
| +0.10 | +0.099873 | 0.127% | +1.597890 | 20027 |
| -0.10 | -0.099888 | 0.112% | -1.646848 | 20022 |
| +0.20 | +0.200069 | 0.035% | +1.582778 | 9997 |
| -0.20 | -0.199847 | 0.077% | -1.636940 | 10008 |
| +0.40 | +0.399545 | 0.114% | +1.568006 | 5006 |
| -0.40 | -0.399626 | 0.094% | -1.624420 | 5005 |
| +0.50 | +0.500056 | 0.011% | +1.567881 | 4000 |
| -0.50 | -0.499818 | 0.036% | -1.616141 | 4002 |

拟合结果：

- 正向库仑电流 `1.579139 A`，RMSE `0.012405 A`；
- 反向库仑电流 `1.631087 A`，RMSE `0.011731 A`；
- 两侧粘性系数经非负约束后均为 0；
- 固件拟合与主机独立拟合在脚本容差内一致。

以 `Kt = 1.5 × 21 × 0.0175 = 0.55125 N·m/A` 换算，库仑阻尼约为正向 `0.871 N·m`、反向 `0.899 N·m`。这是当前整机和阻尼环在这些速度点下的等效结果，不应直接视为裸电机空载摩擦。

应用与掉电参数路径验证：

- `\\w_fap=1` 应用候选模型，再进入 Mode 9 保存参数；
- J-Link 复位前后 `friction_model_valid=1`；
- 复位后活动参数为 `1.579139 / 1.631087 A`，两侧粘性系数均为 0；
- 电流限幅和速度限幅恢复为 `6.00 A / 0.50 rev/s`。

## 位置闭环回归

使用活动摩擦模型、2 A 测试电流限幅、0.05 rev/s 最大速度、0.125 rev/s² 加减速，在 Mode 18 下各执行一次 `±0.02 rev` 低速相对位置移动：

| 方向 | 起点 (rad) | 目标 (rad) | 终点 (rad) | 最终误差 (rad) | 结果 |
| --- | ---: | ---: | ---: | ---: | --- |
| 正向 | +0.1630 | +0.2887 | +0.2860 | +0.0027 | 通过 |
| 反向 | +0.2860 | +0.1603 | +0.1640 | -0.0037 | 通过 |

两次运动期间均保持 Mode 18、Error 0；结束后回到 Mode 0、Error 0，并恢复测试前的控制参数。`tools/usb_position_test.py` 默认在运动期间监控 Mode/Error，停机后读取最终位置；`--live-position` 仅用于诊断，以避免 Windows USB CDC 在高频连续查询时偶发端点超时。

## 构建与实时性

- Keil ARMCC 5.06u7 全量重建：0 errors、0 warnings；Code 104212 B，RO-data 6068 B，RW-data 508 B，ZI-data 29148 B；
- Python 工具测试：3/3 通过；模块边界测试：2/2 通过；
- 架构检查：125 个工程源文件，0 failure，0 warning；
- Mode 19 全程后的 20 kHz 快环：最大 4430 cycles，截止预算 8500 cycles，占 52.1%，deadline overrun 0。

CAN 协议新增的 `0x58`～`0x62` 命令已通过工程全量编译和 Router/Protocol 静态检查；当前电脑未检测到 PCAN、ZLG 等 CAN 适配器，因此本轮未宣称物理 CAN 总线收发通过。无阻尼器配置已由 Product Profile 主机测试覆盖，但由于实机当前安装约 1.5 N·m 阻尼环，未执行无阻尼器机械回归。

两轮原始辨识输出分别位于 `validation/main_mode19_2026-09-05/mode19_result.json` 和 `mode19_applied_result.json`；升级前后 LUT 导出保留在同目录，未纳入版本库。
