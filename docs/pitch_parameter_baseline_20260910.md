# Pitch 控制参数持久化基线

2026-09-10，用户明确要求恢复上一轮已验证的参数并存入 Flash，已完成写入及 MCU 重启回读验证。

| 参数 | Flash 保存值 | 重启后 RAM 读回 |
| --- | ---: | ---: |
| cascade_pos_Kp | 8 | 8 |
| cascade_pos_Kd | 2 | 2 |
| speed_Kp | 0.5 | 0.5 |
| speed_Ki | 2 | 2 |

此前“优化大惯量位置控制器”会话使用了该组 RAM 参数，但没有保存到 Flash。原 Flash 为位置 0.05/0.5、速度 0.05/0.5；本次替换这四个字段，避免重启后恢复旧增益。本轮噪声调试中的位置 4/1、速度 0.3/2 仅为临时对照，不是当前持久化基线。

当前板卡：J-Link 601012403，STM32G431CB，pitch AXS1 身份记录，参数 schema 10。保持 AXS1 格式，通过其原有持久化增益加载路径启动；未执行 AXS2 身份迁移。行程 −0.3～+0.9 rad、最大速度 45°/s 和电流限制 6 A 保持原值。

写入前按匹配 ELF 的 DWARF 解析字段偏移，核对固件、pitch 身份、参数备份和输出关闭。仅重写地址 0x0801C800 的 2048 字节参数页；页中其他数据原样保留。整片 128 KiB Flash 在写入后及重启后均与预期逐字节一致：只有四个增益字段内的 10 个字节变化，程序、编码器零位/LUT、电流校准、身份和行程参数未变化。

重启后确认 Mode=0、Error=0、HIL inactive、CCER=0x1000（三相使能位全部关闭）。本次未下发运动指令；这是持久化及启动加载验证，不是新增运动验收。

当前运行 HIL 镜像 AXF SHA-256：`f6578e7a2d7015f2a70684f79ad62e07cd2b52b2b154397e9282a0e2c008d818`。

保存后的参数区 SHA-256：`8246b1a61b950f4665f8059d79b4963927df2a10abc97e60a3913bdfcfac3b4c`。

本地证据：`outputs/pitch_tuning_20260910_211701/saved_pitch_baseline/`，包含 `manifest.json`、匹配 ELF 导出的偏移、整片 Flash 写入前后备份和参数页文件。当前调试 session 的 `expected_parameters.bin` 已更新；以前会话的参数备份保留为历史证据，不应用其覆盖当前已保存基线。
