--- a/yg_esc_app/application/lifecycle/app_lifecycle.c
+++ b/yg_esc_app/application/lifecycle/app_lifecycle.c
@@ -1,228 +1,496 @@
 #include "app_lifecycle.h"
+
 #include <stddef.h>
 
-#define CANCEL_ACTIONS (APP_ACTION_DISABLE_POWER|APP_ACTION_REVOKE_REQUESTS|APP_ACTION_CANCEL_CONTROL|APP_ACTION_CANCEL_OPERATION)
+/* 停止/故障时统一撤销的请求与作业。 */
+#define CANCEL_ACTIONS                                                                             \
+    (APP_ACTION_DISABLE_POWER | APP_ACTION_REVOKE_REQUESTS | APP_ACTION_CANCEL_CONTROL |           \
+     APP_ACTION_CANCEL_OPERATION)
 #define ALL_EVENTS UINT32_C(0x3fff)
-#define COMPLETIONS (APP_EVENT_BOOT_DONE|APP_EVENT_SELF_TEST_DONE|APP_EVENT_START_DONE|APP_EVENT_OPERATION_DONE|APP_EVENT_OPERATION_FAILED|APP_EVENT_CANCEL_DONE)
-#define REQUESTS (APP_EVENT_ENABLE|APP_EVENT_CLEAR|APP_EVENT_BEGIN_OPERATION)
+/* 异步完成通知：必须匹配阶段 epoch。 */
+#define COMPLETIONS                                                                                \
+    (APP_EVENT_BOOT_DONE | APP_EVENT_SELF_TEST_DONE | APP_EVENT_START_DONE |                       \
+     APP_EVENT_OPERATION_DONE | APP_EVENT_OPERATION_FAILED | APP_EVENT_CANCEL_DONE)
+/* 新请求：必须匹配 request_epoch 且 request_fresh。 */
+#define REQUESTS (APP_EVENT_ENABLE | APP_EVENT_CLEAR | APP_EVENT_BEGIN_OPERATION)
 
+/* 资源静默：相已关、控制空闲、操作空闲、维护资源已释放。 */
 static bool quiet(const AppLifecycleGuards *g)
-{return g->phases_disabled && g->control_idle && g->operation_idle && g->maintenance_released;}
+{
+    return g->phases_disabled && g->control_idle && g->operation_idle && g->maintenance_released;
+}
+/* 健康：样本新鲜、故障源已恢复且无活动故障。 */
 static bool healthy(const AppLifecycleGuards *g)
-{return g->samples_fresh && g->fault_sources_clear && !g->active_faults;}
+{
+    return g->samples_fresh && g->fault_sources_clear && !g->active_faults;
+}
+/* 启动许可：健康 + 角色许可 + 无操作占用。 */
 static bool start_allowed(const AppLifecycleGuards *g)
-{return healthy(g) && g->start_permitted && g->operation_idle && g->maintenance_released;}
+{
+    return healthy(g) && g->start_permitted && g->operation_idle && g->maintenance_released;
+}
+/* 事件集必须恰好一位（完成/请求不允许同拍叠加）。 */
 static bool one_bit(uint32_t value)
-{return value && !(value & (value-1U));}
-static void latch(AppLifecycle *a,uint32_t faults)
-{
-    if(!a->snapshot.first_fault) a->snapshot.first_fault=faults & (0U-faults);
-    a->snapshot.latched_faults|=faults;
-}
-static void fatal(AppLifecycle *a,uint32_t *actions)
-{
-    latch(a,APP_FAULT_INTERNAL);
-    a->snapshot.state=APP_FATAL;a->snapshot.control_mode=APP_CONTROL_NONE;
-    *actions|=CANCEL_ACTIONS;
-}
-static bool advance(AppLifecycle *a,uint32_t *actions)
-{
-    if(a->snapshot.epoch==UINT32_MAX) {fatal(a,actions);return false;}
-    ++a->snapshot.epoch;*actions|=APP_ACTION_REVOKE_REQUESTS;return true;
-}
+{
+    return value && !(value & (value - 1U));
+}
+/* 锁存故障：首故障取最低位，活动故障按位累计。 */
+static void latch(AppLifecycle *a, uint32_t faults)
+{
+    if (!a->snapshot.first_fault)
+    {
+        a->snapshot.first_fault = faults & (0U - faults);
+    }
+    a->snapshot.latched_faults |= faults;
+}
+/* API 误用/不可恢复：锁内部故障并终止，仅复位可退出。 */
+static void fatal(AppLifecycle *a, uint32_t *actions)
+{
+    latch(a, APP_FAULT_INTERNAL);
+    a->snapshot.state = APP_FATAL;
+    a->snapshot.control_mode = APP_CONTROL_NONE;
+    *actions |= CANCEL_ACTIONS;
+}
+/* 推进代际：epoch 用尽锁 FATAL，不回绕。 */
+static bool advance(AppLifecycle *a, uint32_t *actions)
+{
+    if (a->snapshot.epoch == UINT32_MAX)
+    {
+        fatal(a, actions);
+        return false;
+    }
+    ++a->snapshot.epoch;
+    *actions |= APP_ACTION_REVOKE_REQUESTS;
+    return true;
+}
+/* 请求取消当前操作：效果标记未知——取消无法撤销外部已提交写入。 */
 static void cancel_operation(AppLifecycle *a)
 {
-    if(a->snapshot.operation!=APP_OPERATION_NONE &&
-       a->snapshot.operation_result==APP_OPERATION_NO_RESULT) {
-        a->snapshot.last_operation=a->snapshot.operation;
-        a->snapshot.operation_result=APP_OPERATION_CANCEL_REQUESTED;
-        /* Unknown is intentional: cancellation cannot undo an external commit. */
-        a->snapshot.operation_effects=APP_EFFECT_UNKNOWN;
-    }
-}
-static void fault(AppLifecycle *a,uint32_t faults,uint32_t *actions)
-{
-    latch(a,faults);*actions|=APP_ACTION_DISABLE_POWER;
-    if(a->snapshot.state!=APP_FAULT) {
-        cancel_operation(a);*actions|=CANCEL_ACTIONS;
-        if(!advance(a,actions)) return;
-        a->snapshot.state=APP_FAULT;a->snapshot.control_mode=APP_CONTROL_NONE;
-    }
-}
-static void self_test(AppLifecycle *a,uint32_t *actions)
-{
-    if(!advance(a,actions)) return;
-    a->tested=false;a->snapshot.state=APP_SELF_TEST;
-    a->snapshot.control_mode=APP_CONTROL_NONE;a->snapshot.operation=APP_OPERATION_NONE;
-    *actions|=APP_ACTION_DISABLE_POWER|APP_ACTION_SELF_TEST;
-}
-static void stopping(AppLifecycle *a,uint32_t *actions)
-{
-    *actions|=APP_ACTION_DISABLE_POWER;
-    if(a->snapshot.state==APP_FAULT) { *actions|=APP_ACTION_CANCEL_CONTROL|APP_ACTION_CANCEL_OPERATION;return; }
-    if(a->snapshot.state!=APP_STOPPING) {
-        cancel_operation(a);*actions|=CANCEL_ACTIONS;
-        if(!advance(a,actions)) return;
-        a->snapshot.state=APP_STOPPING;a->snapshot.control_mode=APP_CONTROL_NONE;
-    }
-}
-static void confirm_stopped(AppLifecycle *a,const AppLifecycleGuards *g,uint32_t *actions)
-{
-    if(a->snapshot.state!=APP_STOPPING || !quiet(g) ||
-       a->snapshot.operation_result==APP_OPERATION_CANCEL_REQUESTED) return;
-    a->snapshot.operation=APP_OPERATION_NONE;
-    if(a->snapshot.latched_faults) a->snapshot.state=APP_FAULT;
-    else if(a->tested) a->snapshot.state=APP_READY;
-    else self_test(a,actions); /* STOP during boot cannot bypass self-test. */
+    if (a->snapshot.operation != APP_OPERATION_NONE &&
+        a->snapshot.operation_result == APP_OPERATION_NO_RESULT)
+    {
+        a->snapshot.last_operation = a->snapshot.operation;
+        a->snapshot.operation_result = APP_OPERATION_CANCEL_REQUESTED;
+        a->snapshot.operation_effects = APP_EFFECT_UNKNOWN;
+    }
+}
+/* 故障入口：立即关相、撤销作业；已处 FAULT 时只累计锁存。 */
+static void fault(AppLifecycle *a, uint32_t faults, uint32_t *actions)
+{
+    latch(a, faults);
+    *actions |= APP_ACTION_DISABLE_POWER;
+    if (a->snapshot.state != APP_FAULT)
+    {
+        cancel_operation(a);
+        *actions |= CANCEL_ACTIONS;
+        if (!advance(a, actions))
+        {
+            return;
+        }
+        a->snapshot.state = APP_FAULT;
+        a->snapshot.control_mode = APP_CONTROL_NONE;
+    }
+}
+/* 进入自检：推进代际后请求关相与自检。 */
+static void self_test(AppLifecycle *a, uint32_t *actions)
+{
+    if (!advance(a, actions))
+    {
+        return;
+    }
+    a->tested = false;
+    a->snapshot.state = APP_SELF_TEST;
+    a->snapshot.control_mode = APP_CONTROL_NONE;
+    a->snapshot.operation = APP_OPERATION_NONE;
+    *actions |= APP_ACTION_DISABLE_POWER | APP_ACTION_SELF_TEST;
+}
+/* 进入停止：立即关相；FAULT 态只补充取消作业。 */
+static void stopping(AppLifecycle *a, uint32_t *actions)
+{
+    *actions |= APP_ACTION_DISABLE_POWER;
+    if (a->snapshot.state == APP_FAULT)
+    {
+        *actions |= APP_ACTION_CANCEL_CONTROL | APP_ACTION_CANCEL_OPERATION;
+        return;
+    }
+    if (a->snapshot.state != APP_STOPPING)
+    {
+        cancel_operation(a);
+        *actions |= CANCEL_ACTIONS;
+        if (!advance(a, actions))
+        {
+            return;
+        }
+        a->snapshot.state = APP_STOPPING;
+        a->snapshot.control_mode = APP_CONTROL_NONE;
+    }
+}
+/* 停止确认：资源静默且无待收取消时，按锁存/自检状态收口。 */
+static void confirm_stopped(AppLifecycle *a, const AppLifecycleGuards *g, uint32_t *actions)
+{
+    if (a->snapshot.state != APP_STOPPING || !quiet(g) ||
+        a->snapshot.operation_result == APP_OPERATION_CANCEL_REQUESTED)
+    {
+        return;
+    }
+    a->snapshot.operation = APP_OPERATION_NONE;
+    if (a->snapshot.latched_faults)
+    {
+        a->snapshot.state = APP_FAULT;
+    }
+    else if (a->tested)
+    {
+        a->snapshot.state = APP_READY;
+    }
+    else
+    {
+        /* 启动期间 STOP 不能绕过自检。 */
+        self_test(a, actions);
+    }
 }
 void AppLifecycle_Init(AppLifecycle *app)
 {
-    if(app) {const AppLifecycle initial={.snapshot={.state=APP_BOOT,.epoch=1U}};*app=initial;}
-}
-bool AppLifecycle_Read(const AppLifecycle *app,AppLifecycleSnapshot *snapshot)
-{if(!app || !snapshot) return false;*snapshot=app->snapshot;return true;}
+    if (app)
+    {
+        const AppLifecycle initial = {.snapshot = {.state = APP_BOOT, .epoch = 1U}};
+        *app = initial;
+    }
+}
+bool AppLifecycle_Read(const AppLifecycle *app, AppLifecycleSnapshot *snapshot)
+{
+    if (!app || !snapshot)
+    {
+        return false;
+    }
+    *snapshot = app->snapshot;
+    return true;
+}
+AppLifecycleResult
+AppLifecycle_Step(AppLifecycle *a, const AppLifecycleEvent *e, const AppLifecycleGuards *g)
+{
+    AppLifecycleResult r = {.snapshot = {.state = APP_FATAL},
+                            .actions = APP_ACTION_DISABLE_POWER,
+                            .rejection = APP_REJECT_ARGUMENT};
+    uint32_t events, detected, complete, request;
 
-AppLifecycleResult AppLifecycle_Step(AppLifecycle *a,const AppLifecycleEvent *e,
-    const AppLifecycleGuards *g)
-{
-    AppLifecycleResult r={.snapshot={.state=APP_FATAL},.actions=APP_ACTION_DISABLE_POWER,
-                          .rejection=APP_REJECT_ARGUMENT};
-    uint32_t events,detected,complete,request;
-    if(!a) return r;
-    if(!e || !g) {fatal(a,&r.actions);r.snapshot=a->snapshot;return r;}
-    r.actions=0;r.rejection=APP_ACCEPTED;events=e->events;
-    if(a->snapshot.state==APP_FATAL) {r.actions=CANCEL_ACTIONS;r.rejection=APP_REJECT_FATAL;goto done;}
-    if(events & APP_EVENT_FATAL) {latch(a,e->faults);fatal(a,&r.actions);goto done;}
-    detected=g->active_faults | ((events & APP_EVENT_FAULT)?(e->faults?e->faults:APP_FAULT_INTERNAL):0U);
-    /* A valid failure is a fault source, not a discardable completion. Stale
-     * workers cannot fault a new operation. Preserve actual commit uncertainty. */
-    if((events & APP_EVENT_OPERATION_FAILED) && a->snapshot.state==APP_MAINTENANCE &&
-       e->completion_epoch==a->snapshot.epoch && e->operation==a->snapshot.operation) {
-        a->snapshot.last_operation=a->snapshot.operation;
-        a->snapshot.operation_result=APP_OPERATION_FAILED;
-        a->snapshot.operation_effects=(unsigned)e->effects<=APP_EFFECT_COMMITTED?e->effects:APP_EFFECT_UNKNOWN;
-        detected|=e->faults?e->faults:APP_FAULT_OPERATION;
-    }
-    if(detected) {fault(a,detected,&r.actions);r.rejection=APP_REJECT_FAULT;goto done;}
-    if(((a->snapshot.state==APP_STARTING || a->snapshot.state==APP_RUNNING) && !start_allowed(g)) ||
-       (a->snapshot.state==APP_RUNNING && g->phases_disabled &&
-        !(events & (APP_EVENT_STOP|APP_EVENT_CANCEL)))) {
-        fault(a,APP_FAULT_GUARD,&r.actions);r.rejection=APP_REJECT_GUARD;goto done;
-    }
-    if(a->snapshot.state==APP_MAINTENANCE &&
-       (!g->operation_permitted || !g->fault_sources_clear ||
-        (a->snapshot.operation==APP_OPERATION_CALIBRATION?!g->samples_fresh:!g->phases_disabled))) {
-        fault(a,APP_FAULT_GUARD,&r.actions);r.rejection=APP_REJECT_GUARD;goto done;
-    }
-    if(events & APP_EVENT_STOP) {
-        stopping(a,&r.actions);confirm_stopped(a,g,&r.actions);
-        if(events & (COMPLETIONS|REQUESTS)) r.rejection=APP_REJECT_INTERRUPTED;
-        goto done;
-    }
-    if(events & APP_EVENT_CANCEL) {
-        stopping(a,&r.actions);confirm_stopped(a,g,&r.actions);
-        if(events & (COMPLETIONS|REQUESTS)) r.rejection=APP_REJECT_INTERRUPTED;
-        goto done;
-    }
-    if(events & ~ALL_EVENTS) {r.rejection=APP_REJECT_EVENT;goto done;}
-    complete=events & COMPLETIONS;request=events & REQUESTS;
-    if(complete && !one_bit(complete)) {r.rejection=APP_REJECT_EVENT;goto done;}
-    if(complete) {
-        if(e->completion_epoch!=a->snapshot.epoch) {r.rejection=APP_REJECT_STALE_EPOCH;goto done;}
-        switch(complete) {
+    if (!a)
+    {
+        return r;
+    }
+    if (!e || !g)
+    {
+        fatal(a, &r.actions);
+        r.snapshot = a->snapshot;
+        return r;
+    }
+    r.actions = 0;
+    r.rejection = APP_ACCEPTED;
+    events = e->events;
+    if (a->snapshot.state == APP_FATAL)
+    {
+        r.actions = CANCEL_ACTIONS;
+        r.rejection = APP_REJECT_FATAL;
+        goto done;
+    }
+    if (events & APP_EVENT_FATAL)
+    {
+        latch(a, e->faults);
+        fatal(a, &r.actions);
+        goto done;
+    }
+    detected = g->active_faults |
+               ((events & APP_EVENT_FAULT) ? (e->faults ? e->faults : APP_FAULT_INTERNAL) : 0U);
+    /* 有效失败是故障源而非可丢弃的完成；陈旧 worker 不能污染新操作；
+     * 保留真实提交不确定性。 */
+    if ((events & APP_EVENT_OPERATION_FAILED) && a->snapshot.state == APP_MAINTENANCE &&
+        e->completion_epoch == a->snapshot.epoch && e->operation == a->snapshot.operation)
+    {
+        a->snapshot.last_operation = a->snapshot.operation;
+        a->snapshot.operation_result = APP_OPERATION_FAILED;
+        a->snapshot.operation_effects =
+            (unsigned)e->effects <= APP_EFFECT_COMMITTED ? e->effects : APP_EFFECT_UNKNOWN;
+        detected |= e->faults ? e->faults : APP_FAULT_OPERATION;
+    }
+    if (detected)
+    {
+        fault(a, detected, &r.actions);
+        r.rejection = APP_REJECT_FAULT;
+        goto done;
+    }
+    if (((a->snapshot.state == APP_STARTING || a->snapshot.state == APP_RUNNING) &&
+         !start_allowed(g)) ||
+        (a->snapshot.state == APP_RUNNING && g->phases_disabled &&
+         !(events & (APP_EVENT_STOP | APP_EVENT_CANCEL))))
+    {
+        fault(a, APP_FAULT_GUARD, &r.actions);
+        r.rejection = APP_REJECT_GUARD;
+        goto done;
+    }
+    if (a->snapshot.state == APP_MAINTENANCE &&
+        (!g->operation_permitted || !g->fault_sources_clear ||
+         (a->snapshot.operation == APP_OPERATION_CALIBRATION ? !g->samples_fresh
+                                                             : !g->phases_disabled)))
+    {
+        fault(a, APP_FAULT_GUARD, &r.actions);
+        r.rejection = APP_REJECT_GUARD;
+        goto done;
+    }
+    if (events & APP_EVENT_STOP)
+    {
+        stopping(a, &r.actions);
+        confirm_stopped(a, g, &r.actions);
+        if (events & (COMPLETIONS | REQUESTS))
+        {
+            r.rejection = APP_REJECT_INTERRUPTED;
+        }
+        goto done;
+    }
+    if (events & APP_EVENT_CANCEL)
+    {
+        stopping(a, &r.actions);
+        confirm_stopped(a, g, &r.actions);
+        if (events & (COMPLETIONS | REQUESTS))
+        {
+            r.rejection = APP_REJECT_INTERRUPTED;
+        }
+        goto done;
+    }
+    if (events & ~ALL_EVENTS)
+    {
+        r.rejection = APP_REJECT_EVENT;
+        goto done;
+    }
+    complete = events & COMPLETIONS;
+    request = events & REQUESTS;
+    if (complete && !one_bit(complete))
+    {
+        r.rejection = APP_REJECT_EVENT;
+        goto done;
+    }
+    if (complete)
+    {
+        if (e->completion_epoch != a->snapshot.epoch)
+        {
+            r.rejection = APP_REJECT_STALE_EPOCH;
+            goto done;
+        }
+        switch (complete)
+        {
         case APP_EVENT_BOOT_DONE:
-            if(a->snapshot.state!=APP_BOOT) r.rejection=APP_REJECT_STATE;
-            else if(!quiet(g)) r.rejection=APP_REJECT_GUARD;
-            else self_test(a,&r.actions);
+            if (a->snapshot.state != APP_BOOT)
+            {
+                r.rejection = APP_REJECT_STATE;
+            }
+            else if (!quiet(g))
+            {
+                r.rejection = APP_REJECT_GUARD;
+            }
+            else
+            {
+                self_test(a, &r.actions);
+            }
             break;
         case APP_EVENT_SELF_TEST_DONE:
-            if(a->snapshot.state!=APP_SELF_TEST) r.rejection=APP_REJECT_STATE;
-            else if(!g->self_test_passed || !healthy(g) || !quiet(g)) {
-                fault(a,APP_FAULT_GUARD,&r.actions);r.rejection=APP_REJECT_GUARD;
-            } else {a->tested=true;a->snapshot.state=APP_READY;}
+            if (a->snapshot.state != APP_SELF_TEST)
+            {
+                r.rejection = APP_REJECT_STATE;
+            }
+            else if (!g->self_test_passed || !healthy(g) || !quiet(g))
+            {
+                fault(a, APP_FAULT_GUARD, &r.actions);
+                r.rejection = APP_REJECT_GUARD;
+            }
+            else
+            {
+                a->tested = true;
+                a->snapshot.state = APP_READY;
+            }
             break;
         case APP_EVENT_START_DONE:
-            if(a->snapshot.state!=APP_STARTING) r.rejection=APP_REJECT_STATE;
-            else if(!g->startup_confirmed || g->phases_disabled) {
-                fault(a,APP_FAULT_GUARD,&r.actions);r.rejection=APP_REJECT_GUARD;
-            } else a->snapshot.state=APP_RUNNING;
-            break;
-        case APP_EVENT_OPERATION_DONE:case APP_EVENT_OPERATION_FAILED:
-            if(a->snapshot.state!=APP_MAINTENANCE) r.rejection=APP_REJECT_STATE;
-            else if(e->operation!=a->snapshot.operation || (unsigned)e->effects>APP_EFFECT_COMMITTED)
-                r.rejection=APP_REJECT_OPERATION;
-            else if(complete==APP_EVENT_OPERATION_DONE && e->operation==APP_OPERATION_SAVE &&
-                    e->effects!=APP_EFFECT_COMMITTED) r.rejection=APP_REJECT_OPERATION;
-            else {
-                a->snapshot.last_operation=a->snapshot.operation;
-                a->snapshot.operation_effects=e->effects;
-                a->snapshot.operation_result=complete==APP_EVENT_OPERATION_DONE?APP_OPERATION_COMPLETED:APP_OPERATION_FAILED;
-                if(complete==APP_EVENT_OPERATION_FAILED) fault(a,e->faults?e->faults:APP_FAULT_OPERATION,&r.actions);
-                else {stopping(a,&r.actions);confirm_stopped(a,g,&r.actions);}
+            if (a->snapshot.state != APP_STARTING)
+            {
+                r.rejection = APP_REJECT_STATE;
+            }
+            else if (!g->startup_confirmed || g->phases_disabled)
+            {
+                fault(a, APP_FAULT_GUARD, &r.actions);
+                r.rejection = APP_REJECT_GUARD;
+            }
+            else
+            {
+                a->snapshot.state = APP_RUNNING;
+            }
+            break;
+        case APP_EVENT_OPERATION_DONE:
+        case APP_EVENT_OPERATION_FAILED:
+            if (a->snapshot.state != APP_MAINTENANCE)
+            {
+                r.rejection = APP_REJECT_STATE;
+            }
+            else if (e->operation != a->snapshot.operation ||
+                     (unsigned)e->effects > APP_EFFECT_COMMITTED)
+            {
+                r.rejection = APP_REJECT_OPERATION;
+            }
+            else if (complete == APP_EVENT_OPERATION_DONE && e->operation == APP_OPERATION_SAVE &&
+                     e->effects != APP_EFFECT_COMMITTED)
+            {
+                r.rejection = APP_REJECT_OPERATION;
+            }
+            else
+            {
+                a->snapshot.last_operation = a->snapshot.operation;
+                a->snapshot.operation_effects = e->effects;
+                a->snapshot.operation_result = complete == APP_EVENT_OPERATION_DONE
+                                                   ? APP_OPERATION_COMPLETED
+                                                   : APP_OPERATION_FAILED;
+                if (complete == APP_EVENT_OPERATION_FAILED)
+                {
+                    fault(a, e->faults ? e->faults : APP_FAULT_OPERATION, &r.actions);
+                }
+                else
+                {
+                    stopping(a, &r.actions);
+                    confirm_stopped(a, g, &r.actions);
+                }
             }
             break;
         case APP_EVENT_CANCEL_DONE:
-            if(a->snapshot.state!=APP_STOPPING && a->snapshot.state!=APP_FAULT) r.rejection=APP_REJECT_STATE;
-            else if(e->operation!=a->snapshot.operation || (unsigned)e->effects>APP_EFFECT_COMMITTED)
-                r.rejection=APP_REJECT_OPERATION;
-            else if(!quiet(g)) r.rejection=APP_REJECT_BUSY;
-            else {
-                if(a->snapshot.operation_result==APP_OPERATION_CANCEL_REQUESTED) {
-                    a->snapshot.operation_result=APP_OPERATION_CANCELLED;
-                    a->snapshot.operation_effects=e->effects;
+            if (a->snapshot.state != APP_STOPPING && a->snapshot.state != APP_FAULT)
+            {
+                r.rejection = APP_REJECT_STATE;
+            }
+            else if (e->operation != a->snapshot.operation ||
+                     (unsigned)e->effects > APP_EFFECT_COMMITTED)
+            {
+                r.rejection = APP_REJECT_OPERATION;
+            }
+            else if (!quiet(g))
+            {
+                r.rejection = APP_REJECT_BUSY;
+            }
+            else
+            {
+                if (a->snapshot.operation_result == APP_OPERATION_CANCEL_REQUESTED)
+                {
+                    a->snapshot.operation_result = APP_OPERATION_CANCELLED;
+                    a->snapshot.operation_effects = e->effects;
                 }
-                a->snapshot.operation=APP_OPERATION_NONE;
-                confirm_stopped(a,g,&r.actions);
-            }
-            break;
-        default:break;
-        }
-        /* A completion never chains a concurrent request into a fresh start. */
-        if(request && r.rejection==APP_ACCEPTED) r.rejection=APP_REJECT_INTERRUPTED;
-        goto done;
-    }
-    if(request && !one_bit(request)) {r.rejection=APP_REJECT_EVENT;goto done;}
-    if(request && e->request_epoch!=a->snapshot.epoch) {r.rejection=APP_REJECT_STALE_EPOCH;goto done;}
-    if(request && !g->request_fresh) {r.rejection=APP_REJECT_GUARD;goto done;}
-    if(request==APP_EVENT_CLEAR) {
-        if(a->snapshot.state==APP_FAULT) {
-            if(!quiet(g) || !healthy(g) || a->snapshot.operation_result==APP_OPERATION_CANCEL_REQUESTED)
-                r.rejection=APP_REJECT_GUARD;
-            else {
-                /* Advance first: generation exhaustion must retain all faults. */
-                self_test(a,&r.actions);
-                if(a->snapshot.state!=APP_FATAL) {
-                    a->snapshot.first_fault=a->snapshot.latched_faults=0U;
-                    r.actions|=APP_ACTION_FAULT_CLEARED;
+                a->snapshot.operation = APP_OPERATION_NONE;
+                confirm_stopped(a, g, &r.actions);
+            }
+            break;
+        default:
+            break;
+        }
+        /* 完成通知绝不把同拍新请求串成一次新启动。 */
+        if (request && r.rejection == APP_ACCEPTED)
+        {
+            r.rejection = APP_REJECT_INTERRUPTED;
+        }
+        goto done;
+    }
+    if (request && !one_bit(request))
+    {
+        r.rejection = APP_REJECT_EVENT;
+        goto done;
+    }
+    if (request && e->request_epoch != a->snapshot.epoch)
+    {
+        r.rejection = APP_REJECT_STALE_EPOCH;
+        goto done;
+    }
+    if (request && !g->request_fresh)
+    {
+        r.rejection = APP_REJECT_GUARD;
+        goto done;
+    }
+    if (request == APP_EVENT_CLEAR)
+    {
+        if (a->snapshot.state == APP_FAULT)
+        {
+            if (!quiet(g) || !healthy(g) ||
+                a->snapshot.operation_result == APP_OPERATION_CANCEL_REQUESTED)
+            {
+                r.rejection = APP_REJECT_GUARD;
+            }
+            else
+            {
+                /* 先推进代际：generation 用尽时必须保留全部故障。 */
+                self_test(a, &r.actions);
+                if (a->snapshot.state != APP_FATAL)
+                {
+                    a->snapshot.first_fault = a->snapshot.latched_faults = 0U;
+                    r.actions |= APP_ACTION_FAULT_CLEARED;
                 }
             }
-        } else if(a->snapshot.state!=APP_READY && a->snapshot.state!=APP_SELF_TEST)
-            r.rejection=APP_REJECT_STATE;
-    } else if(request) {
-        if(a->snapshot.state!=APP_READY) r.rejection=a->snapshot.latched_faults?APP_REJECT_FAULT:APP_REJECT_STATE;
-        else if(!quiet(g) || !healthy(g) || a->snapshot.latched_faults) r.rejection=APP_REJECT_GUARD;
-        else if(request==APP_EVENT_ENABLE) {
-            if(e->control_mode<=APP_CONTROL_NONE || e->control_mode>APP_CONTROL_VQ) r.rejection=APP_REJECT_MODE;
-            else if(!g->start_permitted) r.rejection=APP_REJECT_GUARD;
-            else if(advance(a,&r.actions)) {
-                a->snapshot.control_mode=e->control_mode;a->snapshot.state=APP_STARTING;
-                r.actions|=APP_ACTION_START_CONTROL;
-            }
-        } else {
-            if(e->operation<=APP_OPERATION_NONE || e->operation>APP_OPERATION_ZERO) r.rejection=APP_REJECT_OPERATION;
-            else if(!g->operation_permitted) r.rejection=APP_REJECT_GUARD;
-            else if(advance(a,&r.actions)) {
-                a->snapshot.operation=e->operation;a->snapshot.last_operation=APP_OPERATION_NONE;
-                a->snapshot.operation_result=APP_OPERATION_NO_RESULT;a->snapshot.operation_effects=APP_EFFECT_UNKNOWN;
-                a->snapshot.state=APP_MAINTENANCE;r.actions|=APP_ACTION_BEGIN_OPERATION;
-            }
-        }
-    } else confirm_stopped(a,g,&r.actions);
+        }
+        else if (a->snapshot.state != APP_READY && a->snapshot.state != APP_SELF_TEST)
+        {
+            r.rejection = APP_REJECT_STATE;
+        }
+    }
+    else if (request)
+    {
+        if (a->snapshot.state != APP_READY)
+        {
+            r.rejection = a->snapshot.latched_faults ? APP_REJECT_FAULT : APP_REJECT_STATE;
+        }
+        else if (!quiet(g) || !healthy(g) || a->snapshot.latched_faults)
+        {
+            r.rejection = APP_REJECT_GUARD;
+        }
+        else if (request == APP_EVENT_ENABLE)
+        {
+            if (e->control_mode <= APP_CONTROL_NONE || e->control_mode > APP_CONTROL_VQ)
+            {
+                r.rejection = APP_REJECT_MODE;
+            }
+            else if (!g->start_permitted)
+            {
+                r.rejection = APP_REJECT_GUARD;
+            }
+            else if (advance(a, &r.actions))
+            {
+                a->snapshot.control_mode = e->control_mode;
+                a->snapshot.state = APP_STARTING;
+                r.actions |= APP_ACTION_START_CONTROL;
+            }
+        }
+        else
+        {
+            if (e->operation <= APP_OPERATION_NONE || e->operation > APP_OPERATION_ZERO)
+            {
+                r.rejection = APP_REJECT_OPERATION;
+            }
+            else if (!g->operation_permitted)
+            {
+                r.rejection = APP_REJECT_GUARD;
+            }
+            else if (advance(a, &r.actions))
+            {
+                a->snapshot.operation = e->operation;
+                a->snapshot.last_operation = APP_OPERATION_NONE;
+                a->snapshot.operation_result = APP_OPERATION_NO_RESULT;
+                a->snapshot.operation_effects = APP_EFFECT_UNKNOWN;
+                a->snapshot.state = APP_MAINTENANCE;
+                r.actions |= APP_ACTION_BEGIN_OPERATION;
+            }
+        }
+    }
+    else
+    {
+        confirm_stopped(a, g, &r.actions);
+    }
 done:
-    if(a->snapshot.state==APP_BOOT || a->snapshot.state==APP_SELF_TEST ||
-       a->snapshot.state==APP_READY || a->snapshot.state==APP_STOPPING ||
-       a->snapshot.state==APP_FAULT || a->snapshot.state==APP_FATAL ||
-       (a->snapshot.state==APP_MAINTENANCE && a->snapshot.operation!=APP_OPERATION_CALIBRATION))
-        r.actions|=APP_ACTION_DISABLE_POWER;
-    r.snapshot=a->snapshot;return r;
-}
+    if (a->snapshot.state == APP_BOOT || a->snapshot.state == APP_SELF_TEST ||
+        a->snapshot.state == APP_READY || a->snapshot.state == APP_STOPPING ||
+        a->snapshot.state == APP_FAULT || a->snapshot.state == APP_FATAL ||
+        (a->snapshot.state == APP_MAINTENANCE &&
+         a->snapshot.operation != APP_OPERATION_CALIBRATION))
+    {
+        r.actions |= APP_ACTION_DISABLE_POWER;
+    }
+    r.snapshot = a->snapshot;
+    return r;
+}
