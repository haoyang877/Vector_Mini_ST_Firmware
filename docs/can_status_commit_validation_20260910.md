# CAN 状态协议、参数基线与故障处理提交

范围：现有参数协议新增 0x64/0x65 周期反馈控制，0x7F0+node 的 32 字节 CAN FD 状态上报；默认预设 20 Hz、上电关闭，可配置 10～200 Hz。数据含故障、模式、外部目标位置/速度、反馈位置/速度、电流参考/反馈和规划位置/速度。编码、遥测快照与 MCU 收发端口分离。

参数：pitch 配置清单同步到已实际保存并重启回读的串级位置 8/2、速度 0.5/2。原始历史参数和本次持久化证据见 `pitch_parameter_baseline_20260910.md`。本提交不修改所有电机的通用默认增益，不触发 Flash 写入，也不把软件配置限位等同于已验证机械行程。

通信修正：TX Queue 使用 pending 位判断是否全空；完整 64 字节 RX 缓冲避免状态帧越界写；停止心跳计时残留、计数回绕和已有故障覆写的回归修正；主机保留 SDK 错误记录并在关闭原会话前采集诊断。原适配器复位后的零计数不能推翻历史错误。

最新实机重试（本次心跳修正下载前）：停机预检通过，零位保持约 0.738 s 后收到 `0x200007F3`；初次 roll 停机回读失败，重建适配器连接并重发 STOP 后两轴均正常回读 mode0/fault0，速度上限恢复 45°/s、反馈流关闭。数据在本地 `outputs/dual_axis_retry_20260910_02/`。新增诊断代码及心跳修正未下载；实际链路故障未宣称解决。

提交不包含工作区另行进行的阻抗控制器重构、schema 10 自动迁移、协议草案、IDE 会话设置和采样窗口配置。验证使用暂存内容导出的独立目录，避免其他未提交修改影响构建结论。历史实测镜像包含当时工作区的其他修改，不能将其等同于本次提交导出的新镜像。

验证命令：

```powershell
python -m unittest discover -s tests -p 'test_*.py'
python tools/run_can_status_tests.py --cc <gcc> --out <output>
```

C 覆盖 7 组：心跳状态转换与饱和、协议编码和全部 191 个帧率、快照字段来源、RX 拒绝路径、硬件端口队列行为、回复优先级、启停/帧率应答。Python 覆盖编解码、双轴边界与分析、SDK ABI/错误标志分离/诊断失败，共 18 项。

以上测试已在暂存导出目录通过；既有位置控制、轨迹、RTT、编码器、HIL 与轴配置原生回归也通过。Keil 普通/HIL 两目标均完整构建，0 错误、0 警告：

| 目标 | Code | RO-data | RW-data | ZI-data |
| --- | ---: | ---: | ---: | ---: |
| 普通版 | 99556 | 5184 | 808 | 30144 |
| HIL | 101276 | 5192 | 1020 | 30188 |

构建日志、测试输出及暂存导出镜像保存在本地 `outputs/can_status_commit_20260910/`；未执行下载或运动测试。
