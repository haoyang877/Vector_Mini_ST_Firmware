# Mode 19 摩擦辨识迁移与实机验证（2026-09-05）

## 结论

主线 `74cbcaa` 的 Mode 19 摩擦辨识已按当前分层架构迁移，主线 `5e88066` 的最高辨识速度调整也已同步为 `0.5 rev/s`。在 28 V 母线、约 1.5 N·m 阻尼环、固件电流限幅 6 A 条件下完成 8/8 个正反速度点，控制器最终回到 Mode 0、Error 0，候选模型未应用。

## 参数兼容验证

schema 从 v8 升级到 v9 后，加载器会校验旧双槽记录的产品、硬件、电机、schema、长度和 CRC，再把 v8 有效负载迁移到 v9 运行时对象。实机按“v8 基线 → 下载 v9 固件 → 保存为 v9 → 复位”验证：

- 编码器在线状态、方向、极对数、电流限幅、速度限幅、Rs、Ld、Lq 和磁链均保持；
- 三次导出的 1024 点 LUT SHA-256 均为 `23BA063CBC2127B375A8C85F5DD2033E039C5C98F7AA6690A4C48AB4B5BA9F21`；
- v9 保存并复位后 LUT 逐点差异为 0；
- 旧 schema 不包含摩擦模型，因此 `friction_model_valid=0`，不会误启用补偿。

## Mode 19 结果

| 目标速度 (rev/s) | 平均速度 (rev/s) | 绝对跟踪误差 | 平均 Iq (A) | 样本数 |
| ---: | ---: | ---: | ---: | ---: |
| +0.10 | +0.100062 | 0.062% | +1.582301 | 19987 |
| -0.10 | -0.099788 | 0.212% | -1.639278 | 20044 |
| +0.20 | +0.200046 | 0.023% | +1.582261 | 9998 |
| -0.20 | -0.199613 | 0.194% | -1.634777 | 10020 |
| +0.40 | +0.399686 | 0.079% | +1.563789 | 5005 |
| -0.40 | -0.399557 | 0.111% | -1.621948 | 5006 |
| +0.50 | +0.500420 | 0.084% | +1.558547 | 3997 |
| -0.50 | -0.499827 | 0.035% | -1.614280 | 4002 |

拟合结果：

- 正向库仑电流 `1.571724 A`，RMSE `0.010718 A`；
- 反向库仑电流 `1.627571 A`，RMSE `0.009966 A`；
- 两侧粘性系数经非负约束后均为 0；
- 固件拟合与主机独立拟合在脚本容差内一致。

以 `Kt = 1.5 × 21 × 0.0175 = 0.55125 N·m/A` 换算，库仑阻尼约为正向 `0.866 N·m`、反向 `0.897 N·m`。这是当前整机和阻尼环在这些速度点下的等效结果，不应直接视为裸电机空载摩擦。

## 构建与实时性

- Keil ARMCLANG：0 errors、0 warnings；Code 104212 B，RO-data 6068 B，RW-data 508 B，ZI-data 29148 B；
- Python 工具测试：3/3 通过；模块边界测试：2/2 通过；
- 架构检查：125 个工程源文件，0 failure，0 warning；
- Mode 19 全程后的 20 kHz 快环：最大 4430 cycles，截止预算 8500 cycles，占 52.1%，deadline overrun 0。

原始辨识输出位于 `validation/main_mode19_2026-09-05/mode19_result.json`；升级前后 LUT 导出保留在同目录，未纳入版本库。
